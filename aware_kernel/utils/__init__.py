"""Utility modules for numerics, linear algebra, and sampling.

This package provides low-level numerical building blocks used across
the library:

* ``linalg``: Safe Cholesky, PCG solver, Frobenius norm, drift computation.
* ``numerics``: Eigenvalue clipping, soft truncation, epsilon scaling,
  conditioning checks.
* ``sampling``: k-means++ and farthest point sampling for landmark/anchor
  selection.
"""
